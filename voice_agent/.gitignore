# The sidecar's dedicated Python venv (created from requirements.txt) and any
# locally cached models are machine-local, never committed.
.venv/
voice-models/
__pycache__/
*.pyc

# Personal config must never be committed (the .example template is fine).
*.local.toml
secrets.toml
voice-chats/
