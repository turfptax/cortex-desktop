# Runtime deps of the Cortex Ingest Agent (the slim exe).
# cortex-intake itself is installed separately with --no-deps (its
# cortexgraph dependency is DirectGateway-only; the agent speaks MCP).
pystray>=0.19
pillow>=10
mcp>=2.0,<3
httpx>=0.27
